/**
 * Entry point for the SchemaTeX webview React application.
 * This file is compiled by TypeScript and bundled for the webview.
 *
 * For development and simple deployments, this creates a simple entry point
 * that can be loaded by the webview HTML.
 */

// This is a placeholder entry point for the webview.
// In a full production build, you would use a module bundler (webpack, esbuild, vite)
// to bundle React, react-dom, and all dependencies together.
//
// For now, the webview uses the HTML fallback approach with VS Code API communication.

console.log('SchemaTeX webview entry point loaded');

